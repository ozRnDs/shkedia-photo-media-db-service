{
    "build": {
        "dockerfile": "Dockerfile",
        "context": "../"
    },
    "forwardPorts": [5555],
    "runArgs": [ "-v", "${localEnv:POSTGRES_CRED_LOCATION}/:/temp/", "-p", "26000:5000"],
    "shutdownAction": "stopContainer"
}